//Trying out code shown in class slides

//var i, number_of_hits = 0, done = true, fruit = "yoghurt";
//let ID = Symbol();
//const pi = Math.PI;

var a = 22;
a *= 3;
alert( a );
//outputs 66 as an alert


if ( num == 12) {
    bool1 = true
} else {
    bool = 2
}

switch (num == 12 ) {
    case true: bool1 = 1;
    break;
    case false: bool1 = 2;
    break;
    default: boo1=???;
}

//use switch cases instead of if else chains
//switch case more compact


//Keyword var declares variables with scope of function they’re in.
//let variables can’t be redeclared in scope. var variables can.

var car = { make: "Ford", model: "Focus", year: 2017 };

//two ways to write the same thing

var person = new Object();
person.firstName = "John";
person.age = 50;



//this is an object
